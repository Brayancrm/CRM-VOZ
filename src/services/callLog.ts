import { Platform } from 'react-native';

export async function getLastCallNumberFromLog(): Promise<string | undefined> {
  return undefined;
}

export async function getLastCallNumberWithRetry(): Promise<string | undefined> {
  return undefined;
}
